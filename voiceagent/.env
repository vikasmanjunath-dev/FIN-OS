# FIN-OS Voice Agent — Environment Variables
# Copy to .env and fill in values

# Supabase — used for persistent cross-session memory
# Use the SERVICE ROLE key so agent can read/write any user's memory
SUPABASE_URL=https://oeapcyucnduhwpgxfknb.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_here
